<%= form_for(@grati) do |f| %>
  <% if @grati.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grati.errors.count, "error") %> prohibited this grati from being saved:</h2>

      <ul>
      <% @grati.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
